## Project Description
(what is this app, who's it for, what does it do — 2-4 sentences)

## Setup / Running Instructions
(clone, install, env vars needed, dev server command, build command)

## Screenshots / GIFs
(placeholder for now — fill in once the dashboard actually renders)

## Rationale
Have raw material for (turn into real sentences):
- Vite: I chose Vite because this project only needs a frontend; the backend already exists as a separate GraphQL API, so I didn't need a framework like Next.js that bundles backend/routing concerns
- TypeScript ensures all data goes accordingly" (fix the typo/"type" mix-up)
- Styling readability, reusability... theme changes propagate automatically" + the system-font-stack reasoning you already explained really well to me earlier in this conversation
- State management (TanStack Query + Context split)
- GraphQL fetch layer
- Component architecture
- Routing 

## Technologies / Libraries Used
(the actual list, once dependencies are finalized)

## Additional Information
(anything else worth mentioning — assumptions you made on ambiguous brief items,
 known gaps, bonus features attempted, etc.)
